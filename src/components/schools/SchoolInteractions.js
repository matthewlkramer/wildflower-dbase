import React from "react";

const SchoolInteractions = ({ school }) => (
  <div>
    <h2 className="text-lg font-semibold mb-4">Emails & Meetings</h2>
    {/* Replace with actual integration logic as needed */}
    <div className="text-gray-500">No interaction data yet.</div>
  </div>
);

export default SchoolInteractions;
